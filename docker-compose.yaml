services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
  backend:
    build:
      context: ./backend-py
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
